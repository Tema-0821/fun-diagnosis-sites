# CLAUDE.md — 재미 진단 사이트 모음 (모노레포)

이 저장소는 "재미형 텍스트 진단" 컨셉으로 여러 개의 독립 사이트를 실험하기 위한 모노레포다.
어떤 주제가 반응이 좋을지 미리 알 수 없어서, 같은 기술 패턴(시드 기반 결정론적 생성)을 재사용해
여러 사이트를 동시에 만들고 각각 별도 도메인으로 배포해서 비교한다.

## 사이트 목록

| 폴더 | 사이트 | 상태 |
|---|---|---|
| `ireum-gunghap/` | **AI궁합** — 이름궁합/MBTI궁합/오늘의운세/연애스타일/짝사랑확률/이상형매치/리더십스타일 (7탭, 랜딩 허브형) | 완료 |
| `nickname-diagnosis/` | 별명 진단 — 별명진단/오늘의기분/리더십스타일 (3탭, TabNav형) | 완료 |
| `work-stress-index/` | 직장 스트레스 지수 — 스트레스지수/번아웃자가진단/힐링방법추천 (3탭, SiteNav형) | 완료 |
| `past-life-fantasy/` | 전생/환생 판타지 진단 — 전생환생/반려몬스터/직업적성 (3탭, SiteNav형), 성별 선택형 AI 일러스트 | 완료 |

**2026-08-22 개편** (애드센스 콘텐츠 빈약 반려 위험 + 사용자 피드백 3건 반영):

1. **궁합 시리즈 병합**: `이름궁합`과 `mbti-gunghap`을 별도 사이트로 배포했으나 "궁합 시리즈는
   하나로 합쳐야" 요청에 따라 `ireum-gunghap/` 폴더 하나로 병합. `mbti-gunghap/` 폴더는 삭제됨 —
   해당 Vercel 프로젝트도 사용자가 수동으로 삭제 필요. 루트(`/`)는 두 진단 중 고르는 랜딩 페이지,
   `/name`과 `/mbti`가 각자의 테마(로맨틱 티켓 vs 플랫 컬러블록)를 유지한 채 서브 라우트로 존재.
   `src/lib/nameCompat/`와 `src/lib/mbtiCompat/`로 로직 네임스페이스 분리, 폰트도
   `--font-heading-name`(Gaegu)/`--font-heading-mbti`(Jua) 두 개를 공존시킴. 결과 카드에는
   `src/components/CompatibilityVenn.tsx`(점수에 따라 겹침 정도가 달라지는 벤 다이어그램 SVG)를
   공통 재사용.
2. **전생환생 전면 재설계 (2차)**: 처음엔 이름 입력 대신 4원소(불/물/바람/대지) 성향 퀴즈로
   바꿨으나, 사용자가 실제 배포 화면을 보고 "운명이 아니라 판타지 세계관 속 종족+직업(엘프 기사,
   고블린 도적, 마왕 등)으로", "전생/환생은 완전히 다른 질문지를 쓰는 별도 탭으로 분리"를 요청 →
   전면 재설계함:
   - `src/lib/pastlife/archetype.ts`: 공용 엔진. 4개 성향 태그(power/wisdom/cunning/charm)로
     6문항 채점 → 1순위 태그가 **직업**, 2순위 태그가 **종족**을 결정. 태그별 종족 4종·직업 4종
     (`pools.ts`)에서 답변 전체를 해시(djb2+mulberry32, `seed.ts`)한 시드로 구체적인 하나를
     골라 조합 — 순위쌍 12가지 × 종족/직업 각 4가지 = 192가지 조합(요구한 "50가지 이상" 충족,
     테스트로 검증됨). 6문항 전부 같은 태그로 답하면 희귀 결과(전생=마왕, 환생=빛의 용사).
   - `pastQuiz.ts`/`rebirthQuiz.ts`: **서로 다른 6문항**을 각각 정의(전생은 "위기 상황에서
     나라면?" 등 과거 성향형 질문, 환생은 "환생한다면 어떤 삶을?" 등 미래 지향형 질문) — 사용자가
     지적한 "환생은 이번 생이 아니라 다음 생" 오타도 수정해서 전부 "환생 후 다음 생에서는"으로
     통일.
   - UI: 화면 상단에 "🕰️ 전생 진단 / ✨ 환생 진단" 탭 스위처를 두고 두 퀴즈를 완전히 독립된
     상태(답변·결과·URL 쿼리)로 운영 — 하나의 결과로 섞이지 않음.
   - `src/components/CharacterPortrait.tsx`: 아이콘이 아니라 **인물 일러스트**를 요청받아, 종족별
     특징(엘프 뾰족귀, 오크 송곳니, 드워프 수염, 마족 뿔, 용족 비늘 등)과 직업별 로브/갑옷 색상 +
     무기 배지를 조합하는 파라메트릭 SVG 캐릭터 흉상을 만듦. 실제 이미지 생성 API가 없는 정적
     사이트라 완전한 렌더링 일러스트는 불가능하다는 점을 사용자에게 설명하고, 대안으로 이 방식을
     택함.
3. **별명진단기 콘텐츠 보강**: 홈페이지에 "이런 원리로 만들어져요" 설명 섹션 + FAQ 4문항 추가
   (office-calc-hub의 AdSense 재승인 성공 패턴 재사용). 결과 카드에는 명사 파트와 매칭되는 이모지
   뱃지(`src/lib/nickname/nounEmoji.ts`, 18개 명사 각각에 이모지 매핑) 추가.

**같은 날 추가 수정 3건**:
- 전생환생: 질문에 악성향(마왕으로 갈 만한) 루트가 없는데 전부 한 태그로 답하면 마왕/빛의 용사가
  나오는 게 "뜬금없다"는 지적 → 희귀 결과 로직 완전히 제거, 질문도 6개→8개로 늘림.
- 결과 이미지: `CharacterPortrait.tsx`(파라메트릭 SVG 인물 흉상)가 "너무 안 어울린다"는 피드백으로
  제거함. 실제 어울리는 이미지가 필요하면 종족별 일러스트를 미리 생성해서(ChatGPT/Stable Diffusion)
  `public/`에 넣고 코드에서 참조하는 방식을 사용자에게 안내함 — 세션 내 자동 생성 불가.
- **[[feedback-guide-faq-separate-tab]] 확립**: 가이드/참고표/FAQ를 메인 도구 페이지에 인라인으로
  붙였다가 "메인 컨텐츠 즐기는 곳에 몰아넣지 말고 별도 탭으로"라는 피드백을 받음 → AI궁합의
  `/name`·`/mbti`, 전생환생의 `/`, 별명진단기의 `/`에서 가이드 콘텐츠를 전부 떼어내
  `/name/guide`, `/mbti/guide`, `/guide`(전생환생·별명진단기)로 분리하고, 메인 페이지에는
  "계산 방식 · FAQ 보기 →" 링크 하나만 남김. **앞으로 만드는 모든 사이트에 이 패턴을 기본 적용할 것**
  (메모리에 저장된 standing rule).
- **AI궁합으로 개명**: `ireum-gunghap/`의 사이트명을 "궁합연구소"에서 **"AI궁합"**으로 변경
  (`layout.tsx`의 `SITE_NAME`, 랜딩 페이지 h1, 뒤로가기 링크, privacy 페이지 전부 반영).

**전생환생 실제 일러스트 추가 (같은 날 후속)**: 사용자가 ChatGPT로 종족별 인물 일러스트를 남/여
각각 생성해서(총 15종족 × 2 = 30장, 유일하게 `거인족`만 이미지가 없어 `RACE_BY_TAG.power`에서
제외함) 전달함.
- 원본은 1254x1254 PNG(장당 ~3MB) — ffmpeg로 480x480 WebP(장당 ~85~115KB)로 리사이즈/압축해서
  `public/portraits/{종족영문키}_{m|f}.webp`로 저장(예: `elf_f.webp`). `RaceInfo`에 `imageKey`
  필드 추가해서 매핑.
- 퀴즈 맨 앞에 "결과 이미지에 사용할 성별" 라디오(남/여)를 추가 — 질문에 답하기 전에 필수 선택.
  선택한 성별은 `?g=m|f`로 URL에 반영되어 공유 링크에서도 동일한 이미지가 재현됨.
  결과 카드에는 `next/image`로 해당 종족+성별 이미지를 원형으로 표시(`priority`를 줘서 즉시 로드).
- **디버깅 메모**: 이 세션의 로컬 브라우저 자동화 도구가 "Browser pane is not displayed"(화면에
  실제로 그려지지 않는 상태)일 때는 `next/image`의 기본 lazy-load가 트리거되지 않아 `naturalWidth`가
  0으로 찍히는 것처럼 보였음 — `priority`를 추가하고 프로덕션 서버로 재확인하니 정상 로드됨(200,
  올바른 파일 크기). 실제 버그가 아니라 도구 쪽 렌더링 타이밍 이슈였음, 비슷한 상황 만나면 우선
  `priority`부터 의심하지 말고 프로덕션 빌드로 검증할 것.

4개 사이트 모두 `npm run lint` / `npm run test` / `npm run build` 통과, 브라우저로 메인 플로우·
공유 링크 재현·모바일 뷰포트(375px)·콘솔 에러 없음까지 확인 완료.

## 공통 원칙

- **각 폴더는 완전히 독립된 Next.js 프로젝트**(자체 `package.json`, `node_modules`). 폴더 안에서 각각
  `npm install`, `npm run dev` 실행.
- **결과 생성은 전부 클라이언트에서, 서버·API 없이**: 입력값(이름, MBTI 등)을 정규화 → 해시로 시드 생성 →
  시드 기반 PRNG로 점수·문구를 결정론적으로 선택. 같은 입력은 항상 같은 결과가 나와야 공유 링크가
  의미 있다. 진짜 AI API는 쓰지 않는다(비용 0원, 서버 불필요, 개인정보가 어디로도 전송되지 않음).
- **결과는 URL 쿼리 파라미터에 반영**해서 링크 공유만으로 같은 결과를 재현할 수 있게 한다(DB 없이
  공유 기능 구현).
- 스택은 office-calc-hub와 동일: Next.js(App Router) + TypeScript + Tailwind CSS v4 + vitest.
- **재미로 보는 콘텐츠라는 면책 문구**를 각 사이트에 꼭 넣는다(과학적 근거 없음 명시).
- **배포**: GitHub 저장소는 이 모노레포 하나만 사용. Vercel에서 사이트마다 별도 프로젝트를 만들고
  "Root Directory"를 각 폴더로 지정해서 독립 도메인으로 배포한다(계정·프로젝트 생성은 사용자가 직접).

**2026-08-22 콘텐츠 탭 10종 추가** (사용자가 제안한 10개 아이디어를 표 순서대로 하나씩 구현,
매번 lint/test/build + 브라우저 검증 후 커밋):

1~5는 이전 세션에 완료(AI궁합 4탭: 오늘의운세/연애스타일/짝사랑확률/이상형매치, 별명진단기
오늘의기분). 이어서 6~10 완료:

6. **나의 반려 몬스터 진단** (`past-life-fantasy/companion`): 4태그(power/wisdom/cunning/charm)
   다수결 방식 6문항 퀴즈. `src/components/SiteNav.tsx`(3탭 Link 네비)를 신설해서 `PastLifeApp.tsx`
   기존 내부 전생/환생 탭 스위처 위에 얹음 — 2단 탭 구조(사이트 전체 콘텐츠 종류 선택 → 그 안에서
   세부 탭).
7. **판타지 직업 적성 테스트** (`past-life-fantasy/job`): 6유형(전사/마법사/궁수/도적/성기사/
   음유시인) 다수결 6문항 퀴즈, SiteNav 3번째 탭으로 연결.
8. **번아웃 자가진단** (`work-stress-index/burnout`): 기존 스트레스지수와 동일한 0~3점 척도 +
   반원 게이지 패턴 재사용. `src/components/SiteNav.tsx` 신설(2탭)로 루트 페이지와 연결.
9. **힐링 방법 추천** (`work-stress-index/healing`): 6유형(자연/취미/휴식/사람들/활동/혼자) 다수결
   6문항 퀴즈. SiteNav를 3탭으로 확장(스트레스지수/번아웃/힐링).
10. **나의 리더십 스타일 진단**: 사용자가 "AI궁합과 별명진단기 둘 다에 각각 별도 구현"으로 선택 →
    같은 퀴즈 콘텐츠(6유형: 카리스마/소통/코칭/서번트/비전/완벽주의, 6문항)를 두 사이트에 독립적으로
    구현. AI궁합은 기존 랜딩 허브에 카드 하나 추가(`/leadership`, sky/blue 테마), 별명진단기는
    `TabNav.tsx`를 3탭으로 확장(`/leadership`).

**디버깅 메모 (브라우저 자동화 도구 클릭 미반영)**: 이 세션 후반, `computer` 툴의 `left_click`이
좌표는 정상 반환하지만 실제 DOM에 클릭 이벤트가 전달되지 않는 구간이 있었음(스크린샷도
"Browser pane is not displayed"로 실패) — 라디오 버튼의 `checked` 상태가 전혀 안 바뀜. 이미 이전에
검증된 `/love-style` 페이지로 재현해봐도 동일하게 실패해서 새로 만든 코드의 버그가 아니라 도구 쪽
렌더링/포커스 이슈로 판단. `javascript_tool`로 `label.querySelector('input').click()`처럼 실제
DOM 클릭 이벤트를 직접 트리거하면 React `onChange`가 정상 동작하는 것을 확인했고, 이 방식으로
우회 검증함. 비슷한 상황(스크린샷 실패 + 클릭 미반영이 동시에 발생)을 만나면 `computer` 툴 대신
`javascript_tool`로 실제 `.click()`을 호출해서 우회할 것.

10개 사이트 모두 `npm run lint` / `npm run test` / `npm run build` 통과 확인. 로컬 커밋만 완료,
원격 push는 사용자 확인 후 진행 예정.

## 참고

`ireum-gunghap/`가 가장 먼저 완성된 레퍼런스 구현이다. 새 사이트를 만들 때는 이 폴더의
`src/lib/*/generate.ts` 패턴(정규화 → djb2 해시 → mulberry32 시드 PRNG → 문장 풀에서 선택)을
그대로 재사용하고, 주제에 맞는 입력값·문장 풀·디자인 톤만 새로 만든다.
