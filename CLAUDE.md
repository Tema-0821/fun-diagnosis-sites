# CLAUDE.md — 재미 진단 사이트 모음 (모노레포)

이 저장소는 "재미형 텍스트 진단" 컨셉으로 여러 개의 독립 사이트를 실험하기 위한 모노레포다.
어떤 주제가 반응이 좋을지 미리 알 수 없어서, 같은 기술 패턴(시드 기반 결정론적 생성)을 재사용해
여러 사이트를 동시에 만들고 각각 별도 도메인으로 배포해서 비교한다.

## 사이트 목록

| 폴더 | 사이트 | 상태 |
|---|---|---|
| `ireum-gunghap/` | **궁합연구소** — 이름궁합(`/name`) + MBTI궁합(`/mbti`) 통합 사이트 | 완료 |
| `nickname-diagnosis/` | 별명(닉네임) 진단 — 결과 이모지 뱃지 + 애드센스 대비 콘텐츠 보강 | 완료 |
| `work-stress-index/` | 직장 스트레스 지수 진단 | 완료 |
| `past-life-fantasy/` | 전생/환생 판타지 진단 — 성향 퀴즈 기반, 전생/환생 결과 분리 | 완료 |

**2026-08-22 개편** (애드센스 콘텐츠 빈약 반려 위험 + 사용자 피드백 3건 반영):

1. **궁합 시리즈 병합**: `이름궁합`과 `mbti-gunghap`을 별도 사이트로 배포했으나 "궁합 시리즈는
   하나로 합쳐야" 요청에 따라 `ireum-gunghap/` 폴더 하나로 병합. `mbti-gunghap/` 폴더는 삭제됨 —
   해당 Vercel 프로젝트도 사용자가 수동으로 삭제 필요. 루트(`/`)는 두 진단 중 고르는 랜딩 페이지,
   `/name`과 `/mbti`가 각자의 테마(로맨틱 티켓 vs 플랫 컬러블록)를 유지한 채 서브 라우트로 존재.
   `src/lib/nameCompat/`와 `src/lib/mbtiCompat/`로 로직 네임스페이스 분리, 폰트도
   `--font-heading-name`(Gaegu)/`--font-heading-mbti`(Jua) 두 개를 공존시킴. 결과 카드에는
   `src/components/CompatibilityVenn.tsx`(점수에 따라 겹침 정도가 달라지는 벤 다이어그램 SVG)를
   공통 재사용.
2. **전생환생 전면 재설계 (2차)**: 처음엔 이름 입력 대신 4원소(불/물/바람/대지) 성향 퀴즈로
   바꿨으나, 사용자가 실제 배포 화면을 보고 "운명이 아니라 판타지 세계관 속 종족+직업(엘프 기사,
   고블린 도적, 마왕 등)으로", "전생/환생은 완전히 다른 질문지를 쓰는 별도 탭으로 분리"를 요청 →
   전면 재설계함:
   - `src/lib/pastlife/archetype.ts`: 공용 엔진. 4개 성향 태그(power/wisdom/cunning/charm)로
     6문항 채점 → 1순위 태그가 **직업**, 2순위 태그가 **종족**을 결정. 태그별 종족 4종·직업 4종
     (`pools.ts`)에서 답변 전체를 해시(djb2+mulberry32, `seed.ts`)한 시드로 구체적인 하나를
     골라 조합 — 순위쌍 12가지 × 종족/직업 각 4가지 = 192가지 조합(요구한 "50가지 이상" 충족,
     테스트로 검증됨). 6문항 전부 같은 태그로 답하면 희귀 결과(전생=마왕, 환생=빛의 용사).
   - `pastQuiz.ts`/`rebirthQuiz.ts`: **서로 다른 6문항**을 각각 정의(전생은 "위기 상황에서
     나라면?" 등 과거 성향형 질문, 환생은 "환생한다면 어떤 삶을?" 등 미래 지향형 질문) — 사용자가
     지적한 "환생은 이번 생이 아니라 다음 생" 오타도 수정해서 전부 "환생 후 다음 생에서는"으로
     통일.
   - UI: 화면 상단에 "🕰️ 전생 진단 / ✨ 환생 진단" 탭 스위처를 두고 두 퀴즈를 완전히 독립된
     상태(답변·결과·URL 쿼리)로 운영 — 하나의 결과로 섞이지 않음.
   - `src/components/CharacterPortrait.tsx`: 아이콘이 아니라 **인물 일러스트**를 요청받아, 종족별
     특징(엘프 뾰족귀, 오크 송곳니, 드워프 수염, 마족 뿔, 용족 비늘 등)과 직업별 로브/갑옷 색상 +
     무기 배지를 조합하는 파라메트릭 SVG 캐릭터 흉상을 만듦. 실제 이미지 생성 API가 없는 정적
     사이트라 완전한 렌더링 일러스트는 불가능하다는 점을 사용자에게 설명하고, 대안으로 이 방식을
     택함.
3. **별명진단기 콘텐츠 보강**: 홈페이지에 "이런 원리로 만들어져요" 설명 섹션 + FAQ 4문항 추가
   (office-calc-hub의 AdSense 재승인 성공 패턴 재사용). 결과 카드에는 명사 파트와 매칭되는 이모지
   뱃지(`src/lib/nickname/nounEmoji.ts`, 18개 명사 각각에 이모지 매핑) 추가.

4개 사이트 모두 `npm run lint` / `npm run test` / `npm run build` 통과, 브라우저로 메인 플로우·
공유 링크 재현·모바일 뷰포트(375px)·콘솔 에러 없음까지 확인 완료.

## 공통 원칙

- **각 폴더는 완전히 독립된 Next.js 프로젝트**(자체 `package.json`, `node_modules`). 폴더 안에서 각각
  `npm install`, `npm run dev` 실행.
- **결과 생성은 전부 클라이언트에서, 서버·API 없이**: 입력값(이름, MBTI 등)을 정규화 → 해시로 시드 생성 →
  시드 기반 PRNG로 점수·문구를 결정론적으로 선택. 같은 입력은 항상 같은 결과가 나와야 공유 링크가
  의미 있다. 진짜 AI API는 쓰지 않는다(비용 0원, 서버 불필요, 개인정보가 어디로도 전송되지 않음).
- **결과는 URL 쿼리 파라미터에 반영**해서 링크 공유만으로 같은 결과를 재현할 수 있게 한다(DB 없이
  공유 기능 구현).
- 스택은 office-calc-hub와 동일: Next.js(App Router) + TypeScript + Tailwind CSS v4 + vitest.
- **재미로 보는 콘텐츠라는 면책 문구**를 각 사이트에 꼭 넣는다(과학적 근거 없음 명시).
- **배포**: GitHub 저장소는 이 모노레포 하나만 사용. Vercel에서 사이트마다 별도 프로젝트를 만들고
  "Root Directory"를 각 폴더로 지정해서 독립 도메인으로 배포한다(계정·프로젝트 생성은 사용자가 직접).

## 참고

`ireum-gunghap/`가 가장 먼저 완성된 레퍼런스 구현이다. 새 사이트를 만들 때는 이 폴더의
`src/lib/*/generate.ts` 패턴(정규화 → djb2 해시 → mulberry32 시드 PRNG → 문장 풀에서 선택)을
그대로 재사용하고, 주제에 맞는 입력값·문장 풀·디자인 톤만 새로 만든다.
